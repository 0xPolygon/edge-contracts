[profile.default]
src = 'contracts'
test = 'test/forge'
libs = ['node_modules', 'lib']
out = 'out'
cache_path  = 'forge-cache'
verbosity = 2
# should work fine; turn off if there are any weird errors
sparse_mode = true

# See more config options https://book.getfoundry.sh/reference/config.html